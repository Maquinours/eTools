<Page
    x:Class="eTools_Ultimate.Views.Pages.GiftboxPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:eTools_Ultimate.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="GiftboxPage"
    d:DataContext="{d:DesignInstance local:GiftboxPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="768"
    d:DesignWidth="1024"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Grid>
        <StackPanel 
            VerticalAlignment="Center" 
            HorizontalAlignment="Center"/>
        <Border
            Grid.Row="0"
            CornerRadius="8" Margin="220,90,220,90">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0" Color="#2B5797" />
                    <GradientStop Offset="1" Color="#0078D7" />
                </LinearGradientBrush>
            </Border.Background>
            <Border CornerRadius="8">
                <Border.Background>
                    <ImageBrush
                        RenderOptions.BitmapScalingMode="HighQuality"
                        Stretch="UniformToFill" ImageSource="/Views/Pages/Giftbox/404Page.png"  />
                </Border.Background>
                <Grid>
                    <StackPanel
                        Margin="48,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Background="Transparent"/>
                    <ui:TextBlock
                        Margin="12"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        FontTypography="Caption"
                        Foreground="#57FFFFFF"
                        Text="eTools Ultimate" />
                </Grid>
            </Border>
        </Border>
    </Grid>
</Page> 