<Page
    x:Class="eTools_Ultimate.Views.Pages.Honor.HonorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:eTools_Ultimate.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:eTools_Ultimate.Views.Pages.Honor"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:eTools_Ultimate.Models"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Honor List"
    d:DataContext="{d:DesignInstance local:HonorPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="791"
    d:DesignWidth="1024"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Page.Resources>
        <!--  Standard Converter  -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!--  Common Styles  -->
        <Style x:Key="InfoIconStyle" TargetType="ui:SymbolIcon">
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Margin" Value="5,0,0,0" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        </Style>
        <Style x:Key="FormFieldStyle" TargetType="Grid">
            <Setter Property="Margin" Value="0,0,15,15" />
        </Style>
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,5" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,15,0,15" />
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Main content  -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>

            <!--  Left side: List of honor entries  -->
            <Grid Grid.Column="0" Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  Search and filter  -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ui:TextBox
                        Grid.Column="0"
                        Margin="0,0,10,0"
                        PlaceholderText="Search by title, ID, category..."
                        Text="{Binding ViewModel.SearchText, UpdateSourceTrigger=PropertyChanged}">
                        <ui:TextBox.Icon>
                            <ui:SymbolIcon Symbol="Search24" />
                        </ui:TextBox.Icon>
                    </ui:TextBox>

                    <Button
                        Grid.Column="1"
                        Command="{Binding ViewModel.ReloadHonorItemsCommand}"
                        ToolTip="Reload honor list">
                        <ui:SymbolIcon Symbol="ArrowSync24" />
                    </Button>
                </Grid>

                <!--  DataGrid for honor entries  -->
                <DataGrid
                    x:Name="HonorsDataGrid"
                    Grid.Row="1"
                    AutoGenerateColumns="False"
                    BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                    BorderThickness="1"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    IsReadOnly="True"
                    ItemsSource="{Binding ViewModel.HonorsView}"
                    SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="60"
                            Binding="{Binding NId}"
                            Header="Index" />
                        <DataGridTextColumn
                            Width="150"
                            Binding="{Binding StrTitle}"
                            Header="Title ID" />
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Title}"
                            Header="Title Name" />
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding NLGrouping}"
                            Header="Category" />
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding NSGrouping}"
                            Header="Sub Category" />
                        <DataGridTextColumn
                            Width="100"
                            Binding="{Binding NNeed}"
                            Header="Req. Value" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!--  Right side: Edit form  -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Header  -->
                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,0,20"
                    Style="{StaticResource SectionHeaderStyle}"
                    Text="Edit Honor Title" />

                <!--  Edit form  -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!--  Index  -->
                        <Grid Style="{StaticResource FormFieldStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Style="{StaticResource FieldLabelStyle}"
                                Text="Index" />
                            <ui:NumberBox
                                Grid.Row="1"
                                Cursor="Arrow"
                                Value="{Binding SelectedItem.NId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ElementName=HonorsDataGrid}" />
                        </Grid>

                        <!--  Title ID  -->
                        <Grid Style="{StaticResource FormFieldStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Style="{StaticResource FieldLabelStyle}"
                                Text="Title ID" />
                            <TextBox Grid.Row="1" Text="{Binding SelectedItem.StrTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ElementName=HonorsDataGrid}" />
                        </Grid>

                        <!--  Title Name  -->
                        <Grid Style="{StaticResource FormFieldStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Style="{StaticResource FieldLabelStyle}"
                                Text="Title Name" />
                            <TextBox Grid.Row="1" Text="{Binding SelectedItem.Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ElementName=HonorsDataGrid}" />
                        </Grid>

                        <!--  Category  -->
                        <Grid Style="{StaticResource FormFieldStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Style="{StaticResource FieldLabelStyle}"
                                Text="Category" />
                            <ComboBox
                                Grid.Row="1"
                                IsEditable="True"
                                Text="{Binding SelectedItem.NLGrouping, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ElementName=HonorsDataGrid}">
                                <ComboBoxItem Content="HI_COUNT_CHECK" />
                                <ComboBoxItem Content="HI_ELASPED_TIME" />
                                <ComboBoxItem Content="HI_USE_ITEM" />
                                <ComboBoxItem Content="HI_EARN_TITLE" />
                                <ComboBoxItem Content="HI_HUNT_MONSTER" />
                            </ComboBox>
                        </Grid>

                        <!--  Sub Category  -->
                        <Grid Style="{StaticResource FormFieldStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Style="{StaticResource FieldLabelStyle}"
                                Text="Sub Category" />
                            <TextBox Grid.Row="1" Text="{Binding SelectedItem.NSGrouping, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ElementName=HonorsDataGrid}" />
                        </Grid>

                        <!--  Required Value  -->
                        <Grid Style="{StaticResource FormFieldStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Style="{StaticResource FieldLabelStyle}"
                                Text="Required Value" />
                            <ui:NumberBox Grid.Row="1" Value="{Binding SelectedItem.NNeed, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ElementName=HonorsDataGrid}" />
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <!--  Buttons  -->
                <StackPanel
                    Grid.Row="2"
                    Margin="0,20,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <Button
                        Margin="0,0,10,0"
                        Command="{Binding ViewModel.AddHonorItemCommand}"
                        Content="New" />
                    <Button
                        Margin="0,0,10,0"
                        Command="{Binding ViewModel.UpdateHonorItemCommand}"
                        Content="Save" />
                    <Button Command="{Binding ViewModel.DeleteHonorItemCommand}" Content="Delete" />
                </StackPanel>
            </Grid>
        </Grid>

        <!--  Status bar  -->
        <Border
            Grid.Row="1"
            Margin="0,20,0,0"
            Padding="10"
            Background="{DynamicResource ControlFillColorDefaultBrush}"
            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{Binding ViewModel.StatusMessage}" />

                <Button
                    Grid.Column="1"
                    Margin="0,0,10,0"
                    Command="{Binding ViewModel.SaveHonorItemsCommand}"
                    Content="Save All Changes" />

                <ProgressBar
                    Grid.Column="2"
                    Width="100"
                    IsIndeterminate="{Binding ViewModel.IsLoading}"
                    Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </Border>
    </Grid>
</Page> 