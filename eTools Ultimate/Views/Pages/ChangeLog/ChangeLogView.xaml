<Page
    x:Class="eTools_Ultimate.Views.Pages.ChangeLog.ChangeLogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:eTools_Ultimate.Converters"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodel="clr-namespace:eTools_Ultimate.ViewModels.Pages.ChangeLog"
    xmlns:i18n="http://schemas.lepo.co/i18n/2022/xaml"
    Title="{i18n:StringLocalizer 'Change Log'}"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Page.Resources>
        <!--  Styles for consistent design  -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        </Style>

        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        </Style>

        <Style x:Key="SecondaryTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        </Style>

        <Style x:Key="CardStyle" TargetType="ui:Card">
            <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
        </Style>

        <!--  DataGrid Styles for better Dark/Light Mode adaptation  -->
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="AlternatingRowBackground" Value="#424242" />
            <Setter Property="RowBackground" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="RowHeight" Value="50" />
        </Style>

        <!--  Verbesserte Stile für DataGrid Spalten  -->
        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="8,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  Type Badge Styles  -->
        <Style x:Key="AddBadgeStyle" TargetType="ui:Badge">
            <Setter Property="Appearance" Value="Success" />
        </Style>

        <Style x:Key="ModifyBadgeStyle" TargetType="ui:Badge">
            <Setter Property="Appearance" Value="Caution" />
        </Style>

        <Style x:Key="DeleteBadgeStyle" TargetType="ui:Badge">
            <Setter Property="Appearance" Value="Danger" />
        </Style>

        <Style x:Key="InfoBadgeStyle" TargetType="ui:Badge">
            <Setter Property="Appearance" Value="Info" />
        </Style>

        <!--  ContextMenu for additional log entry functions  -->
        <ContextMenu x:Key="LogEntryContextMenu">
            <MenuItem Header="{i18n:StringLocalizer 'Restore'}">
                <MenuItem.Icon>
                    <ui:SymbolIcon Symbol="ArrowClockwise24" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="{i18n:StringLocalizer 'Show Details'}">
                <MenuItem.Icon>
                    <ui:SymbolIcon Symbol="Info24" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="{i18n:StringLocalizer 'Before/After Comparison'}">
                <MenuItem.Icon>
                    <ui:SymbolIcon Symbol="ArrowSyncCircle24" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator />
            <MenuItem Header="{i18n:StringLocalizer 'Export Entry Changes'}">
                <MenuItem.Icon>
                    <ui:SymbolIcon Symbol="ArrowUpload24" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="{i18n:StringLocalizer 'Hide from List'}">
                <MenuItem.Icon>
                    <ui:SymbolIcon Symbol="Eye24" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>

        <!--  Converter  -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <converters:ChangeTypeToAppearanceConverter x:Key="ChangeTypeToAppearanceConverter" />

        <!--  Iconstyles für die verschiedenen Änderungstypen  -->
        <Style x:Key="IconBase" TargetType="ui:SymbolIcon">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>

        <Style
            x:Key="AddedIcon"
            BasedOn="{StaticResource IconBase}"
            TargetType="ui:SymbolIcon">
            <Setter Property="Foreground" Value="#4CAF50" />
            <Setter Property="Symbol" Value="Add24" />
        </Style>

        <Style
            x:Key="ModifyIcon"
            BasedOn="{StaticResource IconBase}"
            TargetType="ui:SymbolIcon">
            <Setter Property="Foreground" Value="#FF9800" />
            <Setter Property="Symbol" Value="Wrench24" />
        </Style>

        <Style
            x:Key="DeleteIcon"
            BasedOn="{StaticResource IconBase}"
            TargetType="ui:SymbolIcon">
            <Setter Property="Foreground" Value="#F44336" />
            <Setter Property="Symbol" Value="Delete24" />
        </Style>

        <Style
            x:Key="InfoIcon"
            BasedOn="{StaticResource IconBase}"
            TargetType="ui:SymbolIcon">
            <Setter Property="Foreground" Value="#2196F3" />
            <Setter Property="Symbol" Value="Info24" />
        </Style>
    </Page.Resources>

    <Page.DataContext>
        <viewmodel:ChangeLogViewModel>
            <!--  Setzt die Eigenschaft standardmäßig auf "false", damit Filter ausgeblendet sind  -->
            <viewmodel:ChangeLogViewModel.AreFiltersVisible>False</viewmodel:ChangeLogViewModel.AreFiltersVisible>
        </viewmodel:ChangeLogViewModel>
    </Page.DataContext>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Header - ohne Close-Button  -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Column="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <ui:Button
                    Margin="0,0,12,0"
                    Padding="8,4"
                    Appearance="Transparent"
                    ToolTip="{i18n:StringLocalizer 'Sort: Newest First'}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Margin="0,0,4,0" Symbol="ArrowCircleDown24" />
                        <TextBlock Text="{i18n:StringLocalizer 'Newest First'}" />
                    </StackPanel>
                </ui:Button>
                <ui:Button
                    Margin="0,0,12,0"
                    Padding="8,4"
                    Appearance="Transparent"
                    Command="{Binding ToggleFiltersVisibilityCommand}"
                    ToolTip="{i18n:StringLocalizer 'Show/Hide Filters'}">
                    <ui:SymbolIcon Symbol="Filter24" />
                </ui:Button>
                <ui:Button
                    Margin="0,0,12,0"
                    Padding="8,4"
                    Appearance="Transparent"
                    ToolTip="{i18n:StringLocalizer 'Export Log'}">
                    <ui:SymbolIcon Symbol="ArrowDownload24" />
                </ui:Button>
                <ui:Button
                    Padding="8,4"
                    Appearance="Transparent"
                    Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                    ToolTip="{i18n:StringLocalizer 'Delete Logs'}">
                    <ui:SymbolIcon Symbol="Delete24" />
                </ui:Button>
            </StackPanel>
        </Grid>

        <!--  View Mode & Entry Count  -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Column="0"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <TextBlock
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{i18n:StringLocalizer 'View Mode:'}" />
                <RadioButton
                    Margin="0,0,8,0"
                    Content="{i18n:StringLocalizer 'List'}"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    IsChecked="{Binding IsListViewMode}" />
                <RadioButton
                    Content="{i18n:StringLocalizer 'Accordion'}"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    IsChecked="{Binding IsListViewMode, Converter={StaticResource InverseBooleanConverter}}" />
            </StackPanel>

            <StackPanel
                Grid.Column="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <TextBlock
                    VerticalAlignment="Center"
                    Style="{StaticResource SecondaryTextStyle}"
                    Text="{Binding EntriesCount}" />
                <TextBlock
                    Margin="4,0,0,0"
                    VerticalAlignment="Center"
                    Style="{StaticResource SecondaryTextStyle}"
                    Text="{i18n:StringLocalizer ' entries found'}" />
            </StackPanel>
        </Grid>

        <!--  Search and Filters  -->
        <ui:Card
            Grid.Row="2"
            Margin="0,0,0,12"
            Style="{StaticResource CardStyle}"
            Visibility="{Binding AreFiltersVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Search box  -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ui:TextBox
                        Grid.Column="0"
                        Margin="0,0,8,0"
                        PlaceholderText="{i18n:StringLocalizer 'Search logs...'}">
                        <ui:TextBox.Icon>
                            <ui:SymbolIcon Symbol="Search24" />
                        </ui:TextBox.Icon>
                    </ui:TextBox>

                    <ui:Button
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Appearance="Secondary"
                        Command="{Binding ClearFiltersCommand}"
                        Content="{i18n:StringLocalizer 'Reset Filters'}" />
                </Grid>

                <!--  Filter Fields  -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!--  Item Name Filter  -->
                    <StackPanel
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="0,0,8,12">
                        <TextBlock Style="{StaticResource LabelTextStyle}" Text="{i18n:StringLocalizer 'Item Name'}" />
                        <ui:TextBox PlaceholderText="{i18n:StringLocalizer 'Filter by item name...'}">
                            <ui:TextBox.Icon>
                                <ui:SymbolIcon Symbol="Tag24" />
                            </ui:TextBox.Icon>
                        </ui:TextBox>
                    </StackPanel>

                    <!--  Field Filter  -->
                    <StackPanel
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="0,0,8,12">
                        <TextBlock Style="{StaticResource LabelTextStyle}" Text="{i18n:StringLocalizer 'Field'}" />
                        <ui:TextBox PlaceholderText="{i18n:StringLocalizer 'Filter by field name...'}">
                            <ui:TextBox.Icon>
                                <ui:SymbolIcon Symbol="TextT24" />
                            </ui:TextBox.Icon>
                        </ui:TextBox>
                    </StackPanel>

                    <!--  Value Changed Filter  -->
                    <StackPanel
                        Grid.Row="0"
                        Grid.Column="2"
                        Margin="0,0,0,12">
                        <TextBlock Style="{StaticResource LabelTextStyle}" Text="{i18n:StringLocalizer 'Content'}" />
                        <ui:TextBox PlaceholderText="{i18n:StringLocalizer 'Filter by content change...'}">
                            <ui:TextBox.Icon>
                                <ui:SymbolIcon Symbol="Edit24" />
                            </ui:TextBox.Icon>
                        </ui:TextBox>
                    </StackPanel>

                    <!--  Date From  -->
                    <StackPanel
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="0,0,8,0">
                        <TextBlock Style="{StaticResource LabelTextStyle}" Text="{i18n:StringLocalizer 'Date From'}" />
                        <DatePicker SelectedDate="{Binding DateFrom}" />
                    </StackPanel>

                    <!--  Date To  -->
                    <StackPanel
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="0,0,8,0">
                        <TextBlock Style="{StaticResource LabelTextStyle}" Text="{i18n:StringLocalizer 'Date To'}" />
                        <DatePicker SelectedDate="{Binding DateTo}" />
                    </StackPanel>

                    <!--  Type Filter  -->
                    <StackPanel
                        Grid.Row="1"
                        Grid.Column="2"
                        Margin="0,0,0,0">
                        <TextBlock Style="{StaticResource LabelTextStyle}" Text="{i18n:StringLocalizer 'Change Type'}" />
                        <ComboBox
                            Background="{DynamicResource ControlFillColorDefaultBrush}"
                            DisplayMemberPath="DisplayName"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            ItemsSource="{Binding ChangeTypes}"
                            SelectedItem="{Binding SelectedChangeType}" />
                    </StackPanel>
                </Grid>
            </Grid>
        </ui:Card>

        <!--  Change Log Table  -->
        <ui:Card Grid.Row="3" Style="{StaticResource CardStyle}">
            <Grid>
                <!--  Empty Data Display  -->
                <StackPanel
                    Margin="0,40"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="{Binding HasNoEntries, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ui:SymbolIcon
                        FontSize="48"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Symbol="Document48" />
                    <TextBlock
                        Margin="0,12"
                        HorizontalAlignment="Center"
                        FontSize="16"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{i18n:StringLocalizer 'No log entries found'}" />
                    <TextBlock
                        MaxWidth="400"
                        HorizontalAlignment="Center"
                        Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                        Text="{i18n:StringLocalizer 'Adjust the filters or reset them to see results'}"
                        TextWrapping="Wrap" />
                </StackPanel>

                <!--  DataGrid with improved column styling  -->
                <DataGrid
                    AutoGenerateColumns="False"
                    CellStyle="{StaticResource DataGridCellStyle}"
                    ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                    ContextMenu="{StaticResource LogEntryContextMenu}"
                    HorizontalScrollBarVisibility="Auto"
                    ItemsSource="{Binding LogEntries}"
                    Style="{StaticResource DataGridStyle}"
                    Visibility="{Binding HasEntries, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="Auto"
                            MinWidth="120"
                            Binding="{Binding Timestamp}"
                            Header="{i18n:StringLocalizer 'Timestamp'}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                            <DataGridTextColumn.CellStyle>
                                <Style BasedOn="{StaticResource DataGridCellStyle}" TargetType="DataGridCell">
                                    <Setter Property="Background" Value="{DynamicResource ControlFillColorAltSecondaryBrush}" />
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>

                        <!--  Verbesserte Typ-Spalte mit farbigen Icons  -->
                        <DataGridTemplateColumn
                            Width="Auto"
                            MinWidth="60"
                            Header="{i18n:StringLocalizer 'Type'}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ui:SymbolIcon
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontSize="18">
                                            <ui:SymbolIcon.Style>
                                                <Style TargetType="ui:SymbolIcon">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ChangeType}" Value="Add">
                                                            <Setter Property="Symbol" Value="Add24" />
                                                            <Setter Property="Foreground" Value="#4CAF50" />
                                                            <Setter Property="ToolTip" Value="{i18n:StringLocalizer 'Added'}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ChangeType}" Value="Modify">
                                                            <Setter Property="Symbol" Value="Wrench24" />
                                                            <Setter Property="Foreground" Value="#FF9800" />
                                                            <Setter Property="ToolTip" Value="{i18n:StringLocalizer 'Modified'}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ChangeType}" Value="Delete">
                                                            <Setter Property="Symbol" Value="Delete24" />
                                                            <Setter Property="Foreground" Value="#F44336" />
                                                            <Setter Property="ToolTip" Value="{i18n:StringLocalizer 'Deleted'}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ChangeType}" Value="Info">
                                                            <Setter Property="Symbol" Value="Info24" />
                                                            <Setter Property="Foreground" Value="#2196F3" />
                                                            <Setter Property="ToolTip" Value="{i18n:StringLocalizer 'Information'}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ui:SymbolIcon.Style>
                                        </ui:SymbolIcon>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellStyle>
                                <Style BasedOn="{StaticResource DataGridCellStyle}" TargetType="DataGridCell">
                                    <Setter Property="Background" Value="{DynamicResource ControlFillColorAltSecondaryBrush}" />
                                </Style>
                            </DataGridTemplateColumn.CellStyle>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn
                            Width="*"
                            MinWidth="120"
                            Binding="{Binding Item}"
                            Header="{i18n:StringLocalizer 'Item'}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="FontWeight" Value="Medium" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn
                            Width="*"
                            MinWidth="120"
                            Binding="{Binding Field}"
                            Header="{i18n:StringLocalizer 'Field'}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                            <DataGridTextColumn.CellStyle>
                                <Style BasedOn="{StaticResource DataGridCellStyle}" TargetType="DataGridCell">
                                    <Setter Property="Background" Value="{DynamicResource ControlFillColorAltSecondaryBrush}" />
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>

                        <!--  Kombinierte Value-Spalte mit Pfeil zwischen Old und New  -->
                        <DataGridTemplateColumn
                            Width="*"
                            MinWidth="200"
                            Header="{i18n:StringLocalizer 'Value Change'}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                        <!--  Old Value  -->
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                            Text="{Binding OldValue}"
                                            TextWrapping="Wrap" />

                                        <!--  Arrow - nur anzeigen wenn beide Werte vorhanden sind  -->
                                        <StackPanel Margin="4,0" Orientation="Horizontal">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding NewValue}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding NewValue}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding OldValue}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding OldValue}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <ui:SymbolIcon
                                                VerticalAlignment="Center"
                                                FontSize="14"
                                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                Symbol="ArrowRight24" />
                                        </StackPanel>

                                        <!--  New Value  -->
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                            Text="{Binding NewValue}"
                                            TextWrapping="Wrap" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn
                            Width="Auto"
                            MinWidth="80"
                            Header="{i18n:StringLocalizer 'Actions'}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                        <ui:Button
                                            Margin="0,0,4,0"
                                            Appearance="Transparent"
                                            Command="{Binding DataContext.RestoreEntryCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="{i18n:StringLocalizer 'Restore'}">
                                            <ui:SymbolIcon Symbol="ArrowClockwise24" />
                                        </ui:Button>
                                        <ui:Button
                                            Appearance="Transparent"
                                            Command="{Binding DataContext.ShowDetailsCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="{i18n:StringLocalizer 'Show Details'}">
                                            <ui:SymbolIcon Symbol="Info24" />
                                        </ui:Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </ui:Card>

        <!--  Pagination  -->
        <Grid Grid.Row="4" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Pagination  -->
            <StackPanel
                Grid.Column="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <ui:Button
                    Margin="0,0,4,0"
                    Padding="8,4"
                    Appearance="Secondary"
                    Command="{Binding PreviousPageCommand}"
                    IsEnabled="{Binding CanGoToPreviousPage}">
                    <ui:SymbolIcon Symbol="ChevronLeft24" />
                </ui:Button>

                <TextBlock
                    Margin="4,0,0,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{Binding CurrentPage}" />
                <TextBlock
                    Margin="4,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{i18n:StringLocalizer 'of'}" />
                <TextBlock
                    Margin="0,0,4,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{Binding TotalPages}" />

                <ui:Button
                    Margin="4,0,0,0"
                    Padding="8,4"
                    Appearance="Secondary"
                    Command="{Binding NextPageCommand}"
                    IsEnabled="{Binding CanGoToNextPage}">
                    <ui:SymbolIcon Symbol="ChevronRight24" />
                </ui:Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page> 