<Page
    x:Class="eTools_Ultimate.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:eTools_Ultimate.Views.Pages"
    xmlns:pageViewModels="clr-namespace:eTools_Ultimate.ViewModels.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
    Title="DashboardPage"
    d:DataContext="{d:DesignInstance pageViewModels:DashboardViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="754"
    d:DesignWidth="800"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">


    <Page.Resources>
        <SolidColorBrush x:Key="ApplicationBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="#212121"/>
        <SolidColorBrush x:Key="TextFillColorSecondaryBrush" Color="#9E9E9E"/>



        <!-- Template for navigation cards -->
        <DataTemplate x:Key="NavigationCardTemplate">
            <ui:Card 
                Margin="6"
                Width="230"
                Height="120"
                Cursor="Hand"
                Tag="{Binding}">
                <behaviors:Interaction.Triggers>
                    <behaviors:EventTrigger EventName="MouseLeftButtonUp">
                        <behaviors:InvokeCommandAction Command="{Binding DataContext.OpenCardPageCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}" />
                    </behaviors:EventTrigger>
                </behaviors:Interaction.Triggers>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Left icon - larger size -->
                    <Image 
                        Grid.Column="0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Width="48"
                        Height="48"
                        Margin="4,0,14,0"
                        Source="{Binding Icon}"
                        RenderOptions.BitmapScalingMode="HighQuality" 
                        RenderOptions.EdgeMode="Aliased">
                        <Image.CacheMode>
                            <BitmapCache RenderAtScale="2" />
                        </Image.CacheMode>
                    </Image>

                    <!-- Text in the middle -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock 
                            FontSize="16"
                            FontWeight="Medium"
                            Text="{Binding Title}" />
                        <TextBlock 
                            Margin="0,4,0,0"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{Binding Description}"
                            TextWrapping="Wrap" />
                    </StackPanel>

                    <!-- Right arrow -->
                    <ui:SymbolIcon 
                        Grid.Column="2"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="8,0,0,0"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        FontSize="16"
                        Symbol="ChevronRight24" />
                </Grid>
            </ui:Card>
        </DataTemplate>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Banner styled like the SettingsPage -->
            <Border x:Name="BannerBorder" Grid.Row="1" 
                    Height="300"
                    Margin="0,0,0,0"
                    Cursor="Arrow"
                    CornerRadius="8">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Offset="0" Color="#2B5797" />
                        <GradientStop Offset="1" Color="#0078D7" />
                    </LinearGradientBrush>
                </Border.Background>
                <Border CornerRadius="8">
                    <Border.Background>
                        <ImageBrush 
                            ImageSource="/Assets/Images/HomePage.png" 
                            RenderOptions.BitmapScalingMode="HighQuality"
                            Stretch="UniformToFill" />
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Black banner with blur effect behind Welcome text -->
                        <Border Grid.Row="0"
                                VerticalAlignment="Center"
                                Background="#4C000000"
                                Height="100">
                            <Border.Effect>
                                <BlurEffect Radius="30" KernelType="Gaussian" RenderingBias="Quality" />
                            </Border.Effect>
                        </Border>

                        <StackPanel
                            Grid.Row="0"
                            Margin="48,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Background="Transparent">
                            <ui:TextBlock
                                FontTypography="Title"
                                Foreground="#FFFFFF" 
                                FontWeight="Bold"
                                FontSize="32" Text="Welcome" />
                        </StackPanel>

                        <Grid Grid.Row="1">
                            <ui:TextBlock
                                Margin="12"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                FontTypography="Caption"
                                Foreground="#BFFFFFFF"
                                Text="Created by eTools Ultimate" />

                            <ui:Button
                                Margin="12"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Bottom"
                                Appearance="Primary"
                                Background="#4DFFFFFF"
                                BorderBrush="#33FFFFFF"
                                Foreground="#FFFFFF"
                                Content="Patchlogs"
                                Icon="History24"
                                Command="{Binding OpenPatchNotesDialogCommand}" />
                        </Grid>
                    </Grid>
                </Border>
            </Border>

            <!-- Navigation Cards without inner ScrollViewer -->
            <Grid Grid.Row="2" 
                 Margin="0,24,0,0" 
                 HorizontalAlignment="Stretch" 
                 VerticalAlignment="Top">
                <ItemsControl 
                    Margin="0,0,0,50" 
                    ItemTemplate="{StaticResource NavigationCardTemplate}"
                    ItemsSource="{Binding NavigationCards}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>

            <!-- GitHub Button -->
            <Image Grid.Row="2"
                   Source="/eTools Ultimate;component/Assets/Icons/github.png" 
                   Width="36" 
                   Height="36" 
                   Stretch="Uniform"
                   Margin="0,0,60,20"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Cursor="Hand"
                   ToolTip="View on GitHub"
                   RenderOptions.BitmapScalingMode="HighQuality">
                <Image.InputBindings>
                    <MouseBinding Gesture="LeftClick" Command="{Binding OpenGithubLinkCommand}" />
                </Image.InputBindings>
                <Image.Effect>
                    <DropShadowEffect ShadowDepth="1" Opacity="0.3" BlurRadius="4" Color="#000000"/>
                </Image.Effect>
            </Image>

            <!-- Discord Button -->
            <Image Grid.Row="2"
                   Source="/eTools Ultimate;component/Assets/Icons/discord.png" 
                   Width="36" 
                   Height="36" 
                   Stretch="Uniform"
                   Margin="0,0,20,20"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Cursor="Hand"
                   ToolTip="Join our Discord server"
                   RenderOptions.BitmapScalingMode="HighQuality">
                <Image.InputBindings>
                    <MouseBinding Gesture="LeftClick" Command="{Binding OpenDiscordLinkCommand}" />
                </Image.InputBindings>
                <Image.Effect>
                    <DropShadowEffect ShadowDepth="1" Opacity="0.3" BlurRadius="4" Color="#000000"/>
                </Image.Effect>
            </Image>

            <!-- Changelog-Dialog (hidden) -->
            <Frame x:Name="ChangelogFrame" 
                   Visibility="Collapsed"
                   Grid.RowSpan="3" />
        </Grid>
    </ScrollViewer>
</Page>
