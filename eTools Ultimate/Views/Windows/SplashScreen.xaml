<Window x:Class="eTools_Ultimate.Views.Windows.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:eTools_Ultimate.ViewModels.Windows"
        xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
        Title="SplashScreen" Height="250" Width="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        d:DataContext="{d:DesignInstance viewmodels:SplashScreenViewModel, IsDesignTimeCreatable=False}"
        mc:Ignorable="d">
    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding LoadCommand}"/>
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>
    <Window.TaskbarItemInfo>
        <TaskbarItemInfo x:Name="taskbarItemInfo" ProgressState="Normal" ProgressValue="{Binding LoadingProgress, Mode=OneWay}" />
    </Window.TaskbarItemInfo>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Image Source="/Assets/Images/SplashScreen.png" 
                   Width="500" 
                   Height="150"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality" />
            <TextBlock
                        x:Name="LoadingText"
                        Text="{Binding LoadingText}" 
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"
                       Foreground="White" FontSize="12"/>
            <ProgressBar Width="400" 
                        Height="5" 
                        IsIndeterminate="True"
                        Margin="0,10,0,0">
                <ProgressBar.Foreground>
                    <SolidColorBrush Color="#FFEB9A"/>
                </ProgressBar.Foreground>
            </ProgressBar>
        </StackPanel>

        <TextBlock Grid.Row="2" 
                   Text="{Binding ViewModel.AppVersion, Mode=OneWay}"
                   HorizontalAlignment="Right" 
                   Margin="0,2,0,0"
                   Foreground="Gray"/>
    </Grid>
</Window> 