<ui:FluentWindow
    x:Class="eTools_Ultimate.Views.Windows.DeletionLogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i18n="http://schemas.lepo.co/i18n/2022/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:local="clr-namespace:eTools_Ultimate.Views.Windows"
    mc:Ignorable="d"
    Title=""
    Height="600"
    Width="1200"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResize"
    Icon="pack://application:,,,/Assets/Icons/eTools.png">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Banner -->
        <Border
            Grid.Row="0"
            Height="120"
            Margin="0,0,0,16"
            CornerRadius="6">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0" Color="#2B5797" />
                    <GradientStop Offset="1" Color="#0078D7" />
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel
                Margin="24,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Background="Transparent">
                <ui:TextBlock
                    FontSize="24"
                    FontTypography="Title"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="{i18n:StringLocalizer 'Deletion Log'}" />
                <ui:TextBlock
                    Margin="0,4,0,0"
                    FontSize="12"
                    Foreground="#CCFFFFFF"
                    Text="{i18n:StringLocalizer 'View and restore deleted assets'}" />
            </StackPanel>
        </Border>

        <DataGrid
            Grid.Row="1"
            Margin="0"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserReorderColumns="True"
            CanUserResizeColumns="True"
            CanUserSortColumns="True"
            GridLinesVisibility="Horizontal"
            HeadersVisibility="Column"
            ItemsSource="{Binding LogEntries}"
            SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="150"
                    Binding="{Binding Date}"
                    Header="{i18n:StringLocalizer 'Date'}" />
                <DataGridTextColumn
                    Width="100"
                    Binding="{Binding Action}"
                    Header="{i18n:StringLocalizer 'Action'}" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding FilePath}"
                    Header="{i18n:StringLocalizer 'File Path'}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap" />
                            <Setter Property="FontFamily" Value="Consolas" />
                            <Setter Property="FontSize" Value="11" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding TargetPath}"
                    Header="{i18n:StringLocalizer 'Target Path'}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap" />
                            <Setter Property="FontFamily" Value="Consolas" />
                            <Setter Property="FontSize" Value="11" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding DeletedBy}"
                    Header="{i18n:StringLocalizer 'Deleted By'}" />
                <DataGridTemplateColumn Width="100" Header="{i18n:StringLocalizer 'Actions'}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:Button
                                Command="{Binding DataContext.UndoEntryCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}"
                                Background="#FF6B35"
                                Foreground="White"
                                BorderThickness="0"
                                CornerRadius="4"
                                Padding="8,4"
                                FontSize="12">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon
                                        Margin="0,0,4,0"
                                        FontSize="12"
                                        Symbol="ArrowUndo24" />
                                    <TextBlock Text="{i18n:StringLocalizer 'Undo'}" />
                                </StackPanel>
                            </ui:Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel
            Grid.Row="2"
            Margin="12"
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <ui:Button
                Margin="0,0,8,0"
                Command="{Binding RefreshCommand}"
                Appearance="Secondary">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon
                        Margin="0,0,4,0"
                        FontSize="14"
                        Symbol="ArrowClockwise24" />
                    <TextBlock Text="{i18n:StringLocalizer 'Refresh'}" />
                </StackPanel>
            </ui:Button>
            <ui:Button
                Command="{Binding CloseCommand}"
                Appearance="Primary">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon
                        Margin="0,0,4,0"
                        FontSize="14"
                        Symbol="Dismiss24" />
                    <TextBlock Text="{i18n:StringLocalizer 'Close'}" />
                </StackPanel>
            </ui:Button>
        </StackPanel>
    </Grid>
</ui:FluentWindow>